# The Nest API reads **`apps/api/.env` only** (not a file at the monorepo root).
#
#   cp apps/api/.env.example apps/api/.env
#
# Then edit `apps/api/.env` (ports, secrets). See retail-ims/README.md.
